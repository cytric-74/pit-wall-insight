/**
 * Analytics calculations (consistency score, pit efficiency, undercut/
 * overcut gain, ...) described in docs/07_DATABASE_SCHEMA.md's mart tables.
 *
 * Not implemented yet — these depend on the analytics marts the backend
 * will eventually expose (apps/backend/app/services/, warehouse/dbt/marts/).
 */

export {};
