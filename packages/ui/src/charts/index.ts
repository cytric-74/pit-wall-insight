/**
 * Chart components (LineChart, AreaChart, BarChart, RadarChart, Heatmap,
 * TelemetryChart, ...) — first-class UI, never instantiated ad hoc inside
 * pages (docs/assets/06_CHART_DESIGN_SYSTEM.md). Backed by Apache ECharts /
 * D3 once implemented; nothing rendered yet.
 */

export {};
