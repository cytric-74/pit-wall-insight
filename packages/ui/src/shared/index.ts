/**
 * Small cross-category components that don't belong to a single feature
 * area (docs/09_COMPONENT_LIBRARY.md lists `shared/` alongside the other
 * component categories without enumerating contents).
 */

export {};
