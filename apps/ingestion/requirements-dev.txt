-r requirements.txt

ruff==0.15.22
mypy==2.3.0
pytest==9.1.1
# Provides type stubs for `requests`, needed for `mypy --strict` to type-check
# apps/ingestion/collectors/ergast_client.py without untyped-import errors.
types-requests==2.32.4.20250913
# Provides type stubs for `pandas`, needed for `mypy --strict` to type-check
# apps/ingestion/collectors/fastf1_client.py's DataFrame handling.
pandas-stubs==2.3.2.250926
