"""Aggregates every v1 resource router.

Empty for now — future routers (dashboard, drivers, constructors, races, ...)
will each be included here as they're built, per docs/08_API_SPECIFICATION.md.
"""

from __future__ import annotations

from fastapi import APIRouter

api_router = APIRouter()
